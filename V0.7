#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/**
 * hello.c
 */
int main(void)
{
    const int num = 5; /*can also use after int*/
    printf ("%d", num) 
    num = 8; /* cannot change value if const variable */
    printf("%d", num);

	return 0;
}
