#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/**
 * hello.c
 */
int main(void)
{
    int luckyNumbers[10]; /* even if you don't know how many items go into the array you still have to define how many # will fit in the array */
    luckyNumbers[1] = 80;
    luckyNumbers[0] = 90;
    printf("%d", luckyNumbers[1]); /* array index's start at 0 */





	return 0;
}
