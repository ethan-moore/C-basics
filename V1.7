#include <stdio.h>
#include <stdlib.h>
#include <math.h>

/**
 * hello.c
 */
struct Student{
char name[50];
char major[50];
int age;
double gpa;
};


int main(){
     
   struct Student student1;
   {
     student1.age = 22;
     student1.gpa = 3.2;
     strcpy( student1.name, "Jim");
     strcpy( student1.major, "CPEN");

     printf("%f", student1.major);

   };

    struct Student student2;
   {
     student1.age = 22;
     student1.gpa = 3.2;
     strcpy( student1.name, "Pam");
     strcpy( student1.major, "CPEN");

     printf("%f", student1.major);

   };
   

	return 0;
}
